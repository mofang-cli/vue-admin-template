const regex = {
  /**
   * 验证是否是ip地址
   */
  ip: /(2(5[0-5]{1}|[0-4]\d{1})|[0-1]?\d{1,2})(\.(2(5[0-5]{1}|[0-4]\d{1})|[0-1]?\d{1,2})){3}/g,
  /**
   * 手机号正则验证，只校验11或者12位数字
   */
  phone: /^[1-9]\d{10,11}$/,
  /**
   * 验证是否是数字
   */
  isNumber: /^[1-9]\d*$/
}

export default regex
